<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>org.cleartk</groupId>
	<artifactId>cleartk-dependencies</artifactId>
	<packaging>pom</packaging>
	<version>0.0.1</version>
	<name>cleartk-dependencies</name>
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
	</properties>
	<modules>
		<module>grmm</module>
		<module>jgrapht</module>
		<module>libsvm</module>
		<module>mallet</module>
		<module>malt</module>
		<module>maxent</module>
		<module>mtj</module>
		<module>opennlp</module>
		<module>snowball</module>
		<module>trove</module>
		<module>weka</module>
	</modules>
	<!-- Maven will append the child artifactId to each of these URLs -->
	<scm>
		<connection>scm:svn:http://cleartk.googlecode.com/svn/trunk/dependencies</connection>
		<developerConnection>scm:svn:https://cleartk.googlecode.com/svn/trunk/dependencies</developerConnection>
		<url>http://cleartk.googlecode.com/svn/trunk/dependencies</url>
	</scm>
	<build>
		<pluginManagement>
			<plugins>
				<!-- Attach the jar file to the artifact -->
				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>build-helper-maven-plugin</artifactId>
					<version>1.5</version>
					<executions>
						<execution>
							<id>attach-artifacts</id>
							<phase>package</phase>
							<goals>
								<goal>attach-artifact</goal>
							</goals>
							<configuration>
								<artifacts>
									<artifact>
										<file>${artifactId}-${version}.jar</file>
										<type>jar</type>
									</artifact>
								</artifacts>
							</configuration>
						</execution>
					</executions>
				</plugin>
			</plugins>
		</pluginManagement>
		<extensions>
			<extension>
				<groupId>org.jvnet.wagon-svn</groupId>
				<artifactId>wagon-svn</artifactId>
				<version>1.9</version>
			</extension>
		</extensions>
	</build>
	<repositories>
		<repository>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
			<id>cleartk-googlecode</id>
			<name>ClearTK Google Code repository</name>
			<url>http://cleartk.googlecode.com/svn/repo/</url>
		</repository>
	</repositories>
	<distributionManagement>
		<repository>
			<uniqueVersion>false</uniqueVersion>
			<id>googlecode</id>
			<url>svn:https://cleartk.googlecode.com/svn/repo</url>
		</repository>
	</distributionManagement>
</project>